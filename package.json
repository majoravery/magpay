{
  "name": "magpay",
  "description": "Magbelle Hair Salon pay slip generator",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "author": "Avery <spacemup@users.noreply.github.com>",
  "engines": {
    "node": "~10.16.0",
    "npm": "~6.9.0"
  },
  "scripts": {
    "start": "docker-compose up",
    "deploy": "docker-compose -f docker-compose.yml -f production.yml build api client nginx && docker-compose -f docker-compose.yml -f production.yml up --no-deps -d api client nginx"
  },
  "devDependencies": {
    "prettier": "~1.19.1"
  }
}
